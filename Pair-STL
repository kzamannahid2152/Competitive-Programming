#include<bits/stdc++.h>
using namespace std;

int main(){

    int size; cin>>size;
    pair<int,string>p[size];
    for(int i=0; i<size; i++){
        int a; string b;
        cin>>a>>b;
        p[i] = {a,b};
    }
    cout<<"After Print\n";
    for(int i=0; i<size; i++){
        cout<<p[i].first<<" "<<p[i].second<<endl;
    }
    
    return 0;
}
